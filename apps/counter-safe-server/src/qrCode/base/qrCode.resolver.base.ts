/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import { QrCode } from "./QrCode";
import { QrCodeCountArgs } from "./QrCodeCountArgs";
import { QrCodeFindManyArgs } from "./QrCodeFindManyArgs";
import { QrCodeFindUniqueArgs } from "./QrCodeFindUniqueArgs";
import { CreateQrCodeArgs } from "./CreateQrCodeArgs";
import { UpdateQrCodeArgs } from "./UpdateQrCodeArgs";
import { DeleteQrCodeArgs } from "./DeleteQrCodeArgs";
import { RegisterFindManyArgs } from "../../register/base/RegisterFindManyArgs";
import { Register } from "../../register/base/Register";
import { ScannedFindManyArgs } from "../../scanned/base/ScannedFindManyArgs";
import { Scanned } from "../../scanned/base/Scanned";
import { Product } from "../../product/base/Product";
import { QrCodeService } from "../qrCode.service";
@graphql.Resolver(() => QrCode)
export class QrCodeResolverBase {
  constructor(protected readonly service: QrCodeService) {}

  async _qrCodesMeta(
    @graphql.Args() args: QrCodeCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @graphql.Query(() => [QrCode])
  async qrCodes(@graphql.Args() args: QrCodeFindManyArgs): Promise<QrCode[]> {
    return this.service.qrCodes(args);
  }

  @graphql.Query(() => QrCode, { nullable: true })
  async qrCode(
    @graphql.Args() args: QrCodeFindUniqueArgs
  ): Promise<QrCode | null> {
    const result = await this.service.qrCode(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => QrCode)
  async createQrCode(@graphql.Args() args: CreateQrCodeArgs): Promise<QrCode> {
    return await this.service.createQrCode({
      ...args,
      data: {
        ...args.data,

        product: args.data.product
          ? {
              connect: args.data.product,
            }
          : undefined,
      },
    });
  }

  @graphql.Mutation(() => QrCode)
  async updateQrCode(
    @graphql.Args() args: UpdateQrCodeArgs
  ): Promise<QrCode | null> {
    try {
      return await this.service.updateQrCode({
        ...args,
        data: {
          ...args.data,

          product: args.data.product
            ? {
                connect: args.data.product,
              }
            : undefined,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.Mutation(() => QrCode)
  async deleteQrCode(
    @graphql.Args() args: DeleteQrCodeArgs
  ): Promise<QrCode | null> {
    try {
      return await this.service.deleteQrCode(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.ResolveField(() => [Register], { name: "registers" })
  async findRegisters(
    @graphql.Parent() parent: QrCode,
    @graphql.Args() args: RegisterFindManyArgs
  ): Promise<Register[]> {
    const results = await this.service.findRegisters(parent.id, args);

    if (!results) {
      return [];
    }

    return results;
  }

  @graphql.ResolveField(() => [Scanned], { name: "scanneds" })
  async findScanneds(
    @graphql.Parent() parent: QrCode,
    @graphql.Args() args: ScannedFindManyArgs
  ): Promise<Scanned[]> {
    const results = await this.service.findScanneds(parent.id, args);

    if (!results) {
      return [];
    }

    return results;
  }

  @graphql.ResolveField(() => Product, {
    nullable: true,
    name: "product",
  })
  async getProduct(@graphql.Parent() parent: QrCode): Promise<Product | null> {
    const result = await this.service.getProduct(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }
}
